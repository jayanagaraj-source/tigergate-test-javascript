apiVersion: apps/v1
kind: Deployment
metadata:
  name: tigergate-test-javascript
spec:
  replicas: 1
  selector:
    matchLabels: { app: tigergate-test-javascript }
  template:
    metadata:
      labels: { app: tigergate-test-javascript }
    spec:
      hostNetwork: true # Deliberately insecure IaC fixture.
      containers:
        - name: app
          image: tigergate-test-javascript:latest
          securityContext:
            privileged: true
            runAsUser: 0
          env:
            - name: DATABASE_PASSWORD
              value: "fixture-password-not-real"
